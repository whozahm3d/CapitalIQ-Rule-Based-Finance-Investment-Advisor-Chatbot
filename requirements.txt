# CapitalIQ — Finance & Investment Advisor Chatbot
# DecodeLabs AI Project 1 | Batch 2026 | Ali Ahmad
#
# This project uses only the Python Standard Library for the core chatbot logic.
# No third-party packages are required to run chatbot/ or tests/.
#
# The packages below are optional — needed only to run the Jupyter Notebook.
# Install with: pip install -r requirements.txt

# ── Jupyter Notebook ────────────────────────────────────────────────────────
jupyter>=1.0.0
notebook>=7.0.0

# ── Testing (optional — pytest provides richer output than unittest alone) ──
pytest>=7.4.0

# ── Code Quality (optional — recommended for portfolio submissions) ──────────
# flake8>=6.0.0
# black>=23.0.0
